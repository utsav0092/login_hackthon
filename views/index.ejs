<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home page</title>
    <!-- <link rel="stylesheet" href="index.css"> -->
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        html,
        body {
            width: 100%;
            height: 100%;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            overflow: hidden;
            background: linear-gradient(-45deg, #ee7752, #e73c7e, #23a6d5, #23d5ab);
            background-size: 400% 400%;
            animation: gradient 15s ease infinite;
        }

        @keyframes gradient {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }


        #big-heading {
            text-align: center;
            font-size: 50px;
            font-weight: 600;
            margin-bottom: 30%;
        }

        #main-container {
            border-radius: 12px;
            border: none;
            max-width: 600px;
            margin: 50px auto;
            padding: 12px;
            width: 50%;
            height: auto;
            text-align: center;
            background-color: seashell;
        }

        button {
            width: 70%;
            padding: 10px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 12px;
            cursor: pointer;
            transition: 0.4s ease;
            margin-bottom: 22px;
        }

        button:hover {
            background-color: #0056b3;
            box-shadow: 2px 2px 12px gray;
        }

        a {
            display: block;
            text-align: center;
            margin-top: 10px;
            color: white;
            text-decoration: none;
            font-size: medium;
            font-weight: 700;
            transition: 0.4s ease;
        }

        /* Responsive design */
        @media (max-width: 600px) {
            #main-container {
                width: 80%;
                margin: 10% auto;
                padding: 10px;
            }

            #inner-container {
                width: 80%;
                margin: 20% auto;
                padding-top: 8px;
            }

            button {
                padding: 8px;
            }

            h1 {
                font-size: 30px;
            }

            #small-heading {
                font-size: 18px;
            }

            a {
                font-size: small;
            }
        }
    </style>
</head>

<body>
    <div id="main-container">
        <h1 id="big-heading">Welcome</h1>
        <button><a href="">Click here to Redirect</a></button>
    </div>
</body>

</html>